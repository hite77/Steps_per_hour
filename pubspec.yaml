name: stepsperhour
description: spaces steps out
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  http: ^0.12.1
  flutter_web_auth: any
  clock: ^1.0.1
  charts_flutter: ^0.9.0
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  shared_preferences: 0.5.7
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
  - secrets.json